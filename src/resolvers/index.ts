import {
  STUDENTS_QUERY_TYPEDEF as SQT,
  STUDENTS_QUERY_allStudents_TYPEDEF as SQAT,
} from './queries/students';

export { default as STUDENTS_QUERY } from './queries/students';
export type STUDENTS_QUERY_TYPEDEF = SQT;
export type STUDENTS_QUERY_allStudents_TYPEDEF = SQAT;
